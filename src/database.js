class Database {
    write(object) {
        // Figure out what table this item should go into...
        // Figure out the columns / values that should be inserted...
        
    }
}

module.exports = {
    Database : Database
}